# 项目改进方案与执行约定

日期：2026-09-21。状态：Astra 设计已记录，等待 Luna 实施与验证。

## 范围

只改本项目，不改毕业论文，不导出 PDF。保留计量分析、探索性间接路径、机器学习与 SHAP 的组合。原数据不覆盖，旧结果保留为历史结果，新增结果必须与本次代码和配置对应。当前 GitHub 目标为私有仓库 https://github.com/Yemyu/ev-purchase-intention，默认分支 main。

分析属于已观察过结果后的项目修订，不称为预注册或独立确认性验证。不以显著性筛选题项、控制变量、路径或样本；不要求新结果复现论文数字。已知旧结果造成的选择局限需要在项目报告中说明。

## 已确认的问题

- CSV 有 622 行、67 列；其中包含历史哑变量与异常值处理列，不能把整个 CSV 直接送进模型。题目原始列中地区缺失 110 条；不因无关地区缺失删除主分析样本。
- 旧代码技术信任采用 Q15、Q16 均值，感知价值采用 Q22；论文采用三题、四题。以题目全文映射，不能按论文题号直接取列。
- Notebook、脚本和结果表的模型配置与控制变量不一致。保留既有输出，但不把它们冒充为当前脚本的运行结果。
- VIF 未加入常数项；异质性 LR 方向及自由度有问题；Notebook 分组人数打印有误。
- ML 表中 SHAP 排名有内部不一致；多分类 SHAP 类别选择与论文解释不一致。当前脚本不能完整复现留存的三模型比较表。
- 既有机器学习结果表可用于追溯，不可在未核对预测与评估口径时认定为新的基准性能。

## 变量定义

题号均指 CSV 原始题号。以题目全文建立显式映射，保留原始列。

- Y：Q21，为智驾功能支付溢价的 1–5 级意愿；不将其改名为实际购车行为。
- 核心技术指标 T：保留 mean(Q15, Q16)，实际含义为技术重要性与安全认可的简约指标。项目可讨论技术信任框架，但必须注明它是代理测量，不是经过验证的完整技术信任量表。Q15 的重要性不等于信任，Q16 的安全认可也不覆盖可靠性等全部维度。
- 核心功能指标 V：mean(Q22, Q23, Q24, Q25)，覆盖自适应巡航、车道保持、自动泊车、拥堵辅助。命名为功能吸引力／功能价值认可代理指标，不将其包装成成本收益权衡的成熟感知价值量表。题目提及购买意愿，需说明与 Y 的概念接近及同源测量局限。
- M1：Q18 驾驶乐趣认可；M2：Q19 出行效率认可。单题测量，不并入 T 或 V。
- 固定背景控制集 C：性别、年龄、学历、收入、驾龄、驾驶频率，沿用现有项目六项。按类别编码使用参照组哑变量；未核对问卷选项前不擅自翻译年龄段、金额或驾龄分界，不把类别码当连续金额或年数。
- 不在主模型自动加入 Q8、Q20 或 Q26–Q29 这类意愿／假设情景题，也不使用历史处理列。它们可能改变研究问题或引入概念重叠。

固定三套规格并全部披露：legacy（原项目 T2 + V1，保留原控制编码用于追溯）；primary（T2 + V4，类别控制）；sensitivity（T3=mean(Q15,Q16,Q17) + V4，类别控制，明确新增的是减轻疲劳认可）。三题版本不因显著或预测更高替换主规格。必要时另对 legacy 的编码差异作解释，不能将多项同时变化归因于单个题项。

先做范围、缺失、重复、题间相关和内部一致性诊断。T2 报题间相关，V4 报内部一致性及题项统计；这些不能独立证明构念效度。发现测量结构不支持复合指标时保存诊断并交回 Astra 决策，不遍历题项组合寻找最佳 p 值。

## 计量主分析

采用有序 Logit：先拟合 Y ~ T + V，再拟合 Y ~ T + V + C。报告样本数、系数、OR、95% CI、p 值、似然、AIC/BIC 和收敛诊断。固定控制集，不按显著性逐步删除。

报告核心变量与 Y 的条件关联。可以在介绍中保留直接路径研究问题，但主回归系数不称为已经识别的因果直接效应，也不自动当作后面中介分解的 c'。必要时报告模型预测的等级概率变化，注明其模型依赖性。

VIF 的辅助回归加入常数项，常数项不作解释变量报告；有序模型本身不能额外加入常数。检查比例优势假设与各阈值二元模型的系数稳定性；阈值诊断不是正式检验的替代。严重不适配或不收敛时停止结论输出，交回 Astra，不静默切换模型。

## 探索性间接路径

保留原五条路径：T→M1→Y、T→M2→Y、V→M1→Y、V→M2→Y、T→V→Y。统一使用带 C 的 OLS 路径分解与受访者级 bootstrap，原始 1–5 分值，不混用有序 Logit 系数相乘。

每条路径分别拟合 M ~ X + C、Y ~ X + M + C、Y ~ X + C，报告 a、b、c、c'、ab、95% bootstrap 区间与有效重抽样次数。前四条不额外控制另一个核心认知变量，避免在 T→M→Y 时未经说明控制另一路径上的 V；因此结果属于各路径单独的探索性分解，可能存在遗漏认知变量混杂，不声称相互独立机制或可相加的贡献。第五条单独解释，不能将五条的 ab 相加。

最终 bootstrap 5000 次，固定种子42；先只作小次数功能检查，最终结果明确标记。拟合失败必须记录，失败比例超过1%停止结果采纳并交回 Astra。优先报告区间，不用 bootstrap 符号比例简单包装成精确 p 值。五条均报告，不筛显著路径；区间属于逐路径区间而非家族同时区间。不报告因果中介占比或完全／部分中介标签，不将 OLS 近似忽略有序间距假设的限制藏起来。

若结果不稳定或区间跨零，模块仍保留，结论为未提供清晰的间接关联证据。没有纵向／实验识别，不能通过更换算法消除这个限制。

## 异质性

降为补充分析。采用同样本有序模型的嵌套比较：受限模型包含群体主项与固定控制项；扩展模型增加 T、V 与群体的交互项。LR 为 2*(ll_unrestricted-ll_restricted)，自由度取实际可识别新增参数数目，两个模型必须收敛。报告多维比较的原始与 Holm 调整 p 值。

类别编码不作未获证实的年龄／收入金额转换。稀疏类别或缺失等级先记录，不为显著性事后合并；需要合并时交回 Astra。修正前旧 LR 结论不沿用。

## 机器学习与解释

假设：T、V 在背景变量之外提供预测信息；随机森林可能捕捉有序 Logit 未表达的非线性，但不预设优胜。

主比较：多数类基准、有序 Logit、随机森林；主规格输入一致，使用相同的有标签样本及固定 StratifiedKFold(5, shuffle=True, random_state=42)。另比较 C 与 C+T+V 的特征增量，M1/M2 只进入预先声明的扩展特征集且各模型同步使用。RF 首轮固定200棵、max_depth=6，其余显式记录，不作大规模搜索。XGBoost 在核心流程验收后作为固定配置扩展。

防泄漏：Y 不插补；学习型插补、编码和任何数据驱动选择仅在训练折拟合。事先确定的逐行题项均值允许直接构造，但明确缺失题项规则。保存折索引与逐样本折外概率。验证概率类别顺序以及 argmax 后的1–5标签映射，防止0–4错位。既有随机森林先全数据 fit 再 cross_val_score 并不自动意味着交叉验证泄漏，诊断须以实际数据流为依据。

主要指标 QWK，附准确率、macro-F1、ordinal MAE、混淆矩阵；AUC 仅作补充并记录多分类算法及加权方式。多数类在每个训练折估计，42.44%仅为全样本描述基准。

采纳标准：所有流水线检查通过；对比采用相同折外样本；结果完整披露。只有 QWK 平均差为正且至少4/5折同向时才描述为本次划分中一致改善，仍不称统计显著优势或独立外部验证。否则报告没有稳定优势。指标差异小不追加反复种子搜索；需要调参或更强推断时先交回 Astra，改为内层选择／外层评估。

SHAP 优先解释 P(Y>=4)，由对应类别概率的 SHAP 求和，核对 base value + SHAP sum 与预测输出一致。只解释该折未用于训练的样本，训练数据决定解释器背景。记录 SHAP 版本、类别顺序、返回数组维度和聚合方法。重要性使用 mean(abs(SHAP))，有符号值用于方向，不混用。检查各折排序，不要求与 OR 排序相同；相关特征间贡献分配不是因果证据。

## 工程结构和验收

保留现有 src 模块框架，增加统一 data/schema/config/evaluation 工具；Notebook 调用模块，不复制建模逻辑。命令行支持 audit、econometrics、mediation、heterogeneity、ml 及 all，不默认运行5000次 bootstrap。

使用项目 .venv，安装与版本锁定只在其中完成。不安装系统开发工具。工作中间文件放项目 .work/ 并忽略；结果按运行目录保存，包含数据 SHA256、代码版本及 dirty 状态、配置、依赖版本、种子、样本排除记录、日志和输出清单。旧 figures 保留历史标识，避免历史图表被当作新运行成功的证据。

关键测试：数据与特征映射、缺失题项策略、类别编码与标签映射、概率和为1、折无重叠、SHAP加和、LR的嵌套关系与参数差、失败不能打印成功。结果统一由表格生成，README不手填未验证指标。不要新增网站、深度学习或部署。

验收结果为可运行项目、可追溯结果表与图、变量说明、简短研究报告、更新 README。论文不修改。完整成功前不覆盖旧结果；无论显著与否都能验收一个诚实、有效的流程。代码和报告可推送原私有仓库的工作分支供审查，保留主分支历史；不改变仓库可见性，不另行公开问卷数据。

## 模型交接

Astra 本阶段负责定义与方案。下一阶段建议 Luna 最高实现并验证；切换由用户完成，不声称自动切换。Luna 遇到测量冲突、收敛失败、需要更改路径或预测指标异常时，保存安全进度并交回 Astra；不得自行扩大规格搜索。最终由 Astra 审查方法、结果及对外结论。
